#include "Basic3DEngine.h"
//#include <GLFW/glfw3.h>
//#include <glad/glad.h>

int Basic3DEngine::initializeWindow()
{
	//if (!glfwInit())
	//	return -1;

	//_window = glfwCreateWindow(_windowWidth, _windowHeight, "Simulated Card Deck Prototype", NULL, NULL);
	//if (!_window)
	//{
	//	glfwTerminate();
	//	return -2;
	//}

	//glfwMakeContextCurrent(_window);

	return 0;
}

int Basic3DEngine::initializeOpenGL()
{
	//if (!gladLoadGL())
	//	return -3;
	return 0;
}